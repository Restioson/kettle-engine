language: java

jdk:
  - oraclejdk8

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/

script:
  - bash ./deploy.sh

env:
  global:
  - ENCRYPTION_LABEL: "2a4085cdbc3d"
  - COMMIT_AUTHOR_EMAIL: "restiosondev@gmail.com"